# Python
__pycache__/
*.pyc

# Private key — never commit
*.pem
*.key
*.der

# User cookie store — session credentials, must not be committed
.session-cookies/

# Local agent install (generated by install.ps1, machine-specific)
.cookie-monster/
